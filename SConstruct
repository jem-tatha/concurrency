env = Environment(CC =        	'g++',
				  CCFLAGS =		'-g',
	              CPPPATH =  	['/usr/include'],
	              LIBS =  		['pthread'],
	              LIBPATH =  	['/usr/lib', '.'])
env.Program('hello', ['hello.cpp'])
